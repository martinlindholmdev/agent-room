# PICKUP — full UX/feature/code review (2026-09-18)

Done: Phase 2 inventory is in inventory.md: every main.tsx control/command template, reverse audit of all 28 node.control actions, seven code findings, and isolated API evidence clearing the Remove wrong-key suspect. No browser clicks this phase and no product changes. Tests: python3 -m pytest tests -q → 172 passed in 25.77s; desktop tsc --noEmit → exit 0, no output.
Next: Phase 3 only — browser user journeys from the plan. Use phase1-environment.md to start the isolated helper (work/venv/bin/python desktop_main.py --home work/desktop-test), seed.sh, and Vite. Continue review/ux-2026-09-18; per the phased plan, PR/merge remains Phase 7. Node PATH: /Users/irislindholm/.local/share/opencode/integrations/playwright/node_modules/node/bin; gh PATH: ~/.local/bin.
Blocking: Nothing blocks the code inventory. Browser/native outcomes remain unverified. Test fixture has three original bindings and one original pending request, but the removed probe persists as an inactive hub session and its rejected request has history; not an exact rollback. Failed outbox count observed was 12 (Phase 1 had 9), cause of the increase not established. Never print/commit ready.json or touch the installed app/live data.
